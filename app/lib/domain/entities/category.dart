class Category {
  String title;
  int color;
  int numberOfNotes;
  int id;
  bool isSource;
  bool isCategory;
  Category(
      {required this.title,
      required this.color,
      required this.id,
      required this.numberOfNotes,
      required this.isSource,
      required this.isCategory});
}
